apiVersion: v2
name: inari-server
description: Inari control-plane API server (inari-server) — tenant/org management, catalog, resources inventory.
type: application
version: 0.1.0  # x-release-please-version
# appVersion tracks the inari-server image released from this repo; kept in
# sync by release-please (yaml extra-files on the root go component).
appVersion: "1.2.0"
maintainers:
  - name: 7K-Inari
    url: https://github.com/7K-Inari
