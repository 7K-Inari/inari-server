apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "inari-server.serviceAccountName" . }}
  labels:
    {{- include "inari-server.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
